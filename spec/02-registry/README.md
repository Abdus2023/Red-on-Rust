# 02-registry — Registry Bindings (pointer)

**DERIVED POINTER — generated by the controlled specification pipeline. Not a normative source.** The authority for anything summarized here is named in the same row; where a projection and an authority differ, the authority governs and `scripts/spec/_gate.py` fails.

*generated by `redonrust-spec-pipeline` v`1.0.0` · stage `PUB` · seed source `Red-on-Rust.md` @ `sha256:2aeb9940665f6ef27bbba9895889e93b8be8c1ba9f5ff0558f2b95fee07a25eb` · render `sha256:fdc38e8194030492…`*

The pipeline's four registries are **projections of the existing registers**, generated into `build/spec/` (§20: extend repository governance, do not create a rival registry).

| registry | rendered file | authority it copies | identities |
|---|---|---|---|
| requirements | `build/spec/requirements.json` | `spec/03-obligation-matrix.md` (compiled: `reg/requirements.json`) | 184 |
| obligations | `build/spec/obligations.json` | `spec/03` + `req/registry.json` (atomic layer) | 184 + 545 atomic |
| terminology | `build/spec/terminology.json` | `term/10-index.json` (data of record `term/_terms.py`) | 86 |
| dependencies | `build/spec/dependencies.json` | `dep/10-graph.json` + `spec/10-index.json` | 34 section edges |

Identity set digest (all listed authorities agree): `sha256:7ae2e7706b8e…` over 184 IDs. Statuses: `SPECIFIED` for every row — the evidence ceiling (`spec/00` §2). Registry generation changed 0 identities, 0 statuses, and left 0 rows without provenance.

Atomic reconciliation: 533 of 545 atomic records are cited by a single parent; the residue is registered in `req/02-compound-not-split.md` (compound statements kept whole) — recorded, not repaired.

`reg/requirements.json` (+ `reg/requirements.schema.json`, `reg/status-transitions.json`) remains the committed registry of record, and `reg/status-transitions.json` remains the only channel in which a status may change. This directory adds no second registry of authority; schemas for the pipeline's own output land in `build/spec/schemas/`.
