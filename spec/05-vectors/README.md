# 05-vectors — Evidence Vector Index (pointer)

**DERIVED POINTER — generated by the controlled specification pipeline. Not a normative source.** The authority for anything summarized here is named in the same row; where a projection and an authority differ, the authority governs and `scripts/spec/_gate.py` fails.

*generated by `redonrust-spec-pipeline` v`1.0.0` · stage `PUB` · seed source `Red-on-Rust.md` @ `sha256:2aeb9940665f6ef27bbba9895889e93b8be8c1ba9f5ff0558f2b95fee07a25eb` · render `sha256:06a56825de98e4fc…`*

Registered evidence fixtures, projected (never authored) from the authorities, in the three families §6 names.

| family | vectors | authority | ids (pipeline-local, additive) |
|---|---|---|---|
| canonical | 2 | spec/01 R-CANON-11 (fixtures) / R-CANON-13 (grammar) / C-02 (stale tags) | VEC-CANON-01, VEC-CANON-02 |
| effects | 25 | spec/08 §1 (16 frozen + 9 addendum tags) + spec/10 verification_tags | VEC-EFFECT-01, VEC-EFFECT-02, VEC-EFFECT-03, VEC-EFFECT-04… |
| persistence | 7 | spec/10-index.json crash_matrix (T0–T6) + spec/01 R-DUR/R-RECOV + audit/persistence-crash-consistency-audit.md | VEC-PERSIST-01, VEC-PERSIST-02, VEC-PERSIST-03, VEC-PERSIST-04… |

Each vector record carries the byte-exact quoted value, its authority document and line range, a text digest, and `status: SPECIFIED`. Nothing here is a test, a harness or an implementation: running any of these fixtures requires the Rust code §21 forbids until M0, so the honest state is *registered, unconsumed*.

Policy as rendered: `invented_vectors` 0, `adjudicated_discrepancies` 0, `source_lines_rewritten` 0.

Where the frozen source carries a byte form the canonical rendering does not (the stale standalone tags registered as `spec/06` C-02), the difference is listed in `build/spec/vectors/canonical.json → discrepancies` for the authority to decide — not repaired.
