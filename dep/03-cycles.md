# dep/03 — Strongly Connected Components and Cycles

GENERATED by `python3 dep/_graph.py --write` — do not edit; edit `dep/_edges.py` and re-run. Convention: **`A -> B` means B depends on A** (the arrow points from the thing depended upon to the thing that depends on it), matching `spec/04`. `mod/18` uses the opposite convention — see `dep/05` V-06.

A strongly connected component (SCC) with more than one member is a set of nodes that mutually depend on each other: none of them can be specified, implemented or tested before the others. Two graphs are examined, because the answer differs:

- the **implementation graph** — the module edges a frozen Cargo edge could actually carry (50 of 137). Cycles here are real import cycles.
- the **full typed graph** — all seven kinds. Cycles here are usually specification cycles: a restatement, an evidence loop, or a cross-reference that has no code edge behind it.

Every cycle below is matched against a family in `dep/_edges.py` `CYCLE_FAMILIES`; the run fails if a cycle matches none, so no cycle goes unjudged.

## 1. Layer 1 — crate graph

**0 non-trivial SCCs.** The frozen crate graph is a DAG. That is what makes the independence checks of `dep/05` §1 meaningful: the prohibitions are satisfiable.

## 2. Layer 2 — module graph

### 2.1 Implementation cycles (edges a frozen crate edge can carry)

11 SCCs, **3 non-trivial**. All of them are inside a single crate, so the crate DAG stays acyclic; they are still real mutual imports within that crate.

#### SCC {MOD-05, MOD-06, MOD-07, MOD-08} — ror-runtime

- family **F-INTRA-RUNTIME** — inside `ror-runtime`: evaluator / actor / scheduler / effect
- 8 edges inside the component:
  - `MOD-05 EVALUATOR -> MOD-06 ACTOR` [RUNTIME_DEPENDENCY, prose]
  - `MOD-05 EVALUATOR -> MOD-07 SCHEDULER` [RUNTIME_DEPENDENCY, prose]
  - `MOD-05 EVALUATOR -> MOD-08 EFFECT` [RUNTIME_DEPENDENCY, prose+1 req]
  - `MOD-06 ACTOR -> MOD-07 SCHEDULER` [RUNTIME_DEPENDENCY, prose+2 req]
  - `MOD-06 ACTOR -> MOD-08 EFFECT` [RUNTIME_DEPENDENCY, prose+1 req]
  - `MOD-07 SCHEDULER -> MOD-06 ACTOR` [RUNTIME_DEPENDENCY, 5 req]
  - `MOD-07 SCHEDULER -> MOD-08 EFFECT` [RUNTIME_DEPENDENCY, prose+1 req]
  - `MOD-08 EFFECT -> MOD-05 EVALUATOR` [RUNTIME_DEPENDENCY, 1 req]
- **verdict:** **IMPLEMENTATION cycle, intra-crate — acceptable but must be documented.** All members live in `ror-runtime`, so no crate cycle arises. The mutual edges are the CEK `Request` transition (evaluation suspends into the request sequence, which resumes the continuation), the actor/scheduler runnable contract, and the `Pending`/wakeup handshake. Name `request.rs` and `transition.rs` as the single owners of the re-entry points so the cycle is a documented re-entry rather than a mutual import.

#### SCC {MOD-01, MOD-04, MOD-10} — ror-core

- family **F-INTRA-CORE** — inside `ror-core`: domain types / budget algebra / canonical format
- 4 edges inside the component:
  - `MOD-01 CORE -> MOD-04 BUDGET` [TYPE_DEPENDENCY, prose+4 req]
  - `MOD-01 CORE -> MOD-10 SERIALIZATION` [SEMANTIC_DEPENDENCY, prose+1 req]
  - `MOD-04 BUDGET -> MOD-01 CORE` [SEMANTIC_DEPENDENCY, 11 req]
  - `MOD-10 SERIALIZATION -> MOD-01 CORE` [TYPE_DEPENDENCY, 3 req]
- **verdict:** Acceptable, intra-crate, and already anticipated: `spec/04` §B L94 flags exactly this risk ('`ror-core` hosting both `Expr`/`Value` and the canonical traits while `ror-runtime` needs canonical marshalling'). `ror-core` is one crate, so the cycle is a mutual reference between type domains inside it, not a Cargo cycle: `MOD-01 <-> MOD-10` is the `Value` (machine) vs `Value` (15A canonical) collision C-03/C-45 -> U-09, and `MOD-04 -> MOD-01` (SEMANTIC, 11 records) is a central-invariant restatement. Resolve U-09 by naming the two domains differently and the mutual reference disappears.

#### SCC {MOD-11, MOD-12} — ror-persistence

- family **F-INTRA-PERSISTENCE** — inside `ror-persistence`: journal vs recovery
- 2 edges inside the component:
  - `MOD-11 PERSISTENCE -> MOD-12 RECOVERY` [PERSISTENCE_DEPENDENCY, prose+11 req]
  - `MOD-12 RECOVERY -> MOD-11 PERSISTENCE` [PERSISTENCE_DEPENDENCY, 6 req]
- **verdict:** Acceptable, intra-crate. The journal chain defines what recovery reads and recovery defines which records are admissible; both are `ror-persistence`. The one thing to fix is U-17 (which of snapshot queue / reconstruction is authoritative), because that decides which side is normative.

### 2.2 Mutual-dependency pairs in the full typed graph

**41 pairs** of modules depend on each other. Grouped by family:

#### F-BUDGET-GATE — budget algebra vs machine gates (2)

| Pair | `A -> B` | `B -> A` |
|---|---|---|
| `MOD-04` ↔ `MOD-06` | RUNTIME [prose+2 req] | SEMANTIC [1 req] |
| `MOD-04` ↔ `MOD-08` | RUNTIME [prose+14 req] | SEMANTIC [4 req] |

**Verdict:** Acceptable. `MOD-04 -> <machine>` is the real per-transition gate call; the reverse edge is the conservation law *stated over* the machine's charging points, which is SEMANTIC only (`ror-core` may not depend on `ror-runtime`, L39820 §14).

#### F-CEILING-OPERAND — ceiling operand: capability algebra vs budget algebra (1)

| Pair | `A -> B` | `B -> A` |
|---|---|---|
| `MOD-03` ↔ `MOD-04` | TYPE [crate-table+prose+2 req] | TYPE [1 req] |

**Verdict:** **Crate-ambiguous — V-09.** A capability's resource ceiling is a budget value and the budget's ceiling operand is a constraint value, so the two algebras are mutually typed. Both are partly in `ror-core`, and `ror-core -> ror-kernel` is forbidden (L39821 §14), so the shared operand type must live in `ror-core`. Not a code cycle once MOD-04's home is fixed.

#### F-CORE-RESTATEMENT — central-invariant restatement (duplication register) (9)

| Pair | `A -> B` | `B -> A` |
|---|---|---|
| `MOD-01` ↔ `MOD-02` | TYPE [crate-table+prose+6 req] | SEMANTIC [7 req] |
| `MOD-01` ↔ `MOD-03` | TYPE [crate-table+prose+11 req] | SEMANTIC [13 req] |
| `MOD-01` ↔ `MOD-05` | TYPE [crate-table+prose+2 req] | SEMANTIC [1 req] |
| `MOD-01` ↔ `MOD-06` | TYPE [crate-table+prose+5 req] | SEMANTIC [8 req] |
| `MOD-01` ↔ `MOD-07` | TYPE [crate-table+prose+2 req] | SEMANTIC [2 req] |
| `MOD-01` ↔ `MOD-08` | TYPE [crate-table+prose+7 req] | SEMANTIC [6 req] |
| `MOD-01` ↔ `MOD-09` | TYPE [crate-table+prose+4 req] | SEMANTIC [5 req] |
| `MOD-01` ↔ `MOD-11` | TYPE [crate-table+prose+4 req] | SEMANTIC [9 req] |
| `MOD-01` ↔ `MOD-12` | TYPE [crate-table+4 req] | SEMANTIC [14 req] |

**Verdict:** Not a code cycle. `MOD-01 -> X` is a TYPE_DEPENDENCY (the module needs the frozen domain types); `X -> MOD-01` is the central invariant restated in the trust/thesis text whose operative statement is single-homed in X (duplication register D-01…D-12, `mod/18` §3). The restatement side carries no code edge; it exists so a reader of the thesis can find the operative rule.

#### F-DURABILITY-JOURNAL — journal chain vs request sequence (1)

| Pair | `A -> B` | `B -> A` |
|---|---|---|
| `MOD-08` ↔ `MOD-11` | SEMANTIC [3 req] | PERSISTENCE [crate-table+prose+8 req] |

**Verdict:** Acceptable, one direction is specification-only. `MOD-11 -> MOD-08` (the effect module depends on the journal: request step 14 calls `ror-persistence` append/sync) is a *crate-level* coupling according to `mod/_ownership.MODULE_DEPS`, but `spec/07` §6 carries no `ror-persistence -> ror-runtime` edge, so it is not realisable today — that gap is V-10/HD-3. The reverse (`ror-persistence` citing the request sequence) is specification-only and recorded as SEMANTIC — `dep/05` HD-1.

#### F-EFFECT-HOST — effect / host handshake (1)

| Pair | `A -> B` | `B -> A` |
|---|---|---|
| `MOD-08` ↔ `MOD-09` | RUNTIME [crate-table+prose+2 req] | RUNTIME [prose+2 req] |

**Verdict:** Acceptable, resolved by dependency inversion. `MOD-08 -> MOD-09` is the real crate edge (`ror-runtime -> ror-host`, adapter boundary): the host implements a trait the runtime defines. The reverse `MOD-09 -> MOD-08` (receipts resume continuations, durable-trace sourcing) is a callback through that trait, so the crate DAG stays acyclic even though the module graph does not.

#### F-ESCROW-DURABILITY — escrow durability / post-crash revalidation (2)

| Pair | `A -> B` | `B -> A` |
|---|---|---|
| `MOD-04` ↔ `MOD-11` | TYPE [1 req] | PERSISTENCE [1 req] |
| `MOD-04` ↔ `MOD-12` | TYPE [prose+1 req] | PERSISTENCE [1 req] |

**Verdict:** **Crate-ambiguous — V-09.** The escrow partition must be durable and must revalidate after recovery, but MOD-04's crate home is 'ror-core (+ gates in ror-runtime / ror-kernel)', so neither direction can be checked against the frozen crate list. Fix MOD-04's home, then this pair resolves to a one-way type dependency plus a specification constraint.

#### F-EVIDENCE-LOOP — evidence loop (module ↔ verification layer) (18)

| Pair | `A -> B` | `B -> A` |
|---|---|---|
| `MOD-01` ↔ `MOD-14` | SEMANTIC [prose+5 req] | VERIFICATION [2 req] |
| `MOD-01` ↔ `MOD-17` | VERIFICATION [15 req] | VERIFICATION [9 req] |
| `MOD-02` ↔ `MOD-17` | VERIFICATION [3 req] | VERIFICATION [1 req] |
| `MOD-03` ↔ `MOD-17` | VERIFICATION [8 req] | VERIFICATION [1 req] |
| `MOD-04` ↔ `MOD-17` | VERIFICATION [1 req] | VERIFICATION [1 req] |
| `MOD-05` ↔ `MOD-17` | VERIFICATION [4 req] | VERIFICATION [1 req] |
| `MOD-06` ↔ `MOD-17` | VERIFICATION [5 req] | VERIFICATION [1 req] |
| `MOD-08` ↔ `MOD-17` | VERIFICATION [3 req] | VERIFICATION [1 req] |
| `MOD-09` ↔ `MOD-15` | VERIFICATION [prose+1 req] | VERIFICATION [1 req] |
| `MOD-10` ↔ `MOD-15` | VERIFICATION [prose+1 req] | VERIFICATION [1 req] |
| `MOD-10` ↔ `MOD-17` | VERIFICATION [5 req] | VERIFICATION [1 req] |
| `MOD-11` ↔ `MOD-17` | VERIFICATION [8 req] | VERIFICATION [1 req] |
| `MOD-12` ↔ `MOD-14` | SEMANTIC [prose+5 req] | VERIFICATION [crate-table+prose+1 req] |
| `MOD-12` ↔ `MOD-17` | VERIFICATION [19 req] | VERIFICATION [2 req] |
| `MOD-14` ↔ `MOD-15` | VERIFICATION [crate-table+prose+2 req] | VERIFICATION [4 req] |
| `MOD-14` ↔ `MOD-17` | VERIFICATION [8 req] | VERIFICATION [5 req] |
| `MOD-15` ↔ `MOD-17` | VERIFICATION [13 req] | VERIFICATION [crate-table+prose+4 req] |
| `MOD-16` ↔ `MOD-17` | VERIFICATION [5 req] | VERIFICATION [crate-table+prose+2 req] |

**Verdict:** Not a code cycle. Both directions are VERIFICATION_DEPENDENCY (or the reference's SEMANTIC mirror): a module's obligations cite the evidence that discharges them, and the verification layer cites the obligations it tests. These edges are test-time only and are excluded from the implementation graph by construction (no frozen crate edge carries them, except `ror-reference -> ror-differential` and `ror-runtime -> ror-differential`, which are dev-dependencies — HD-6).

#### F-HOST-AGENT — end-to-end replay composition (1)

| Pair | `A -> B` | `B -> A` |
|---|---|---|
| `MOD-09` ↔ `MOD-13` | RUNTIME [prose+1 req] | RUNTIME [prose] |

**Verdict:** **REQUIRES ARCHITECTURAL REVIEW.** `mod/09-host.md` declares MOD-13 and `mod/13-agent.md` declares MOD-09 for the same conformance-suite composition; neither `ror-host -> ror-agent` nor `ror-agent -> ror-host` appears in any crate list, and both directions are typed RUNTIME. Pick one owner — the conformance suite (`tests/`) should compose the two — and delete the other prose edge.

#### F-INTRA-CORE — inside `ror-core`: domain types / budget algebra / canonical format (2)

| Pair | `A -> B` | `B -> A` |
|---|---|---|
| `MOD-01` ↔ `MOD-04` | TYPE [prose+4 req] | SEMANTIC [11 req] |
| `MOD-01` ↔ `MOD-10` | SEMANTIC [prose+1 req] | TYPE [3 req] |

**Verdict:** Acceptable, intra-crate, and already anticipated: `spec/04` §B L94 flags exactly this risk ('`ror-core` hosting both `Expr`/`Value` and the canonical traits while `ror-runtime` needs canonical marshalling'). `ror-core` is one crate, so the cycle is a mutual reference between type domains inside it, not a Cargo cycle: `MOD-01 <-> MOD-10` is the `Value` (machine) vs `Value` (15A canonical) collision C-03/C-45 -> U-09, and `MOD-04 -> MOD-01` (SEMANTIC, 11 records) is a central-invariant restatement. Resolve U-09 by naming the two domains differently and the mutual reference disappears.

#### F-INTRA-PERSISTENCE — inside `ror-persistence`: journal vs recovery (1)

| Pair | `A -> B` | `B -> A` |
|---|---|---|
| `MOD-11` ↔ `MOD-12` | PERSISTENCE [prose+11 req] | PERSISTENCE [6 req] |

**Verdict:** Acceptable, intra-crate. The journal chain defines what recovery reads and recovery defines which records are admissible; both are `ror-persistence`. The one thing to fix is U-17 (which of snapshot queue / reconstruction is authoritative), because that decides which side is normative.

#### F-INTRA-RUNTIME — inside `ror-runtime`: evaluator / actor / scheduler / effect (2)

| Pair | `A -> B` | `B -> A` |
|---|---|---|
| `MOD-05` ↔ `MOD-08` | RUNTIME [prose+1 req] | RUNTIME [1 req] |
| `MOD-06` ↔ `MOD-07` | RUNTIME [prose+2 req] | RUNTIME [5 req] |

**Verdict:** **IMPLEMENTATION cycle, intra-crate — acceptable but must be documented.** All members live in `ror-runtime`, so no crate cycle arises. The mutual edges are the CEK `Request` transition (evaluation suspends into the request sequence, which resumes the continuation), the actor/scheduler runnable contract, and the `Pending`/wakeup handshake. Name `request.rs` and `transition.rs` as the single owners of the re-entry points so the cycle is a documented re-entry rather than a mutual import.

#### F-PLANNER-TRUST — trust model vs planner prohibitions (1)

| Pair | `A -> B` | `B -> A` |
|---|---|---|
| `MOD-01` ↔ `MOD-13` | TYPE [crate-table+prose+9 req] | SECURITY [14 req] |

**Verdict:** **REQUIRES ARCHITECTURAL REVIEW — V-03.** The closing edge `MOD-13 -> MOD-01` is a SECURITY_DEPENDENCY whose provider is the LLM/planner module. The properties are prohibitions *on* the planner and enforcement sits at the machine boundary (`spec/07` §3), so the architecture is sound, but as recorded the planner is a provider of a security guarantee. Re-home the operative statement of R-TRUST-01 against MOD-03/MOD-06/MOD-08 and the cycle disappears.

### 2.3 Why the full graph is one component

The full typed graph has **1 non-trivial SCC covering all 17 modules**. That is not a finding about the architecture on its own; most of it is the union of the two systematic families above: every production module is mutually connected to MOD-01 (central-invariant restatement, F-CORE-RESTATEMENT) and to MOD-17 (evidence loop, F-EVIDENCE-LOOP), and MOD-01/MOD-17 touch everything. Deleting those two families removes 27 of the 41 mutual pairs — but **not** the cycle: 14 mutual pairs survive, one SCC still covers all 13 production modules, and the reduced graph still has no root (its only leaves are MOD-16, MOD-17), so Kahn's algorithm places 0 of 17 modules (`dep/02` §2.2). The three implementation cycles of §2.1 are therefore not the whole story: 5 of the surviving pairs are the intra-crate ones of §2.1, and the other 9 close through couplings that mix the specification and implementation layers (F-BUDGET-GATE, F-CEILING-OPERAND, F-DURABILITY-JOURNAL, F-EFFECT-HOST, F-ESCROW-DURABILITY, F-HOST-AGENT, F-PLANNER-TRUST).

| Subgraph | Edges | Non-trivial SCCs | Roots | Leaves |
|---|---|---|---|---|
| full typed graph | 137 | 1 (largest 17) | — | — |
| implementation graph | 50 | 3 (largest 4) | MOD-14, MOD-16, MOD-17 | MOD-09, MOD-13, MOD-15, MOD-16, MOD-17 |
| production modules only (all kinds) | 76 | 1 (largest 13) | — | — |
| production, implementation edges | 45 | 3 (largest 4) | — | MOD-09, MOD-13 |
| verification modules only | 10 | 1 (largest 4) | — | — |
| full graph minus F-CORE-RESTATEMENT + F-EVIDENCE-LOOP (27 pairs deleted) | 83 | 1 (largest 13) | — | MOD-16, MOD-17 |
| kind-filtered subgraph (NOT the implementability test) | 60 | 1 (largest 12) | MOD-10, MOD-14, MOD-15, MOD-16, MOD-17 | MOD-14, MOD-15, MOD-16, MOD-17 |

The last row is why kind alone cannot decide implementability: the 60-edge kind-filtered subgraph has 1 non-trivial SCC but **149 elementary circuits of length 2-5** (9 of length 2, 15 of length 3, 38 of length 4, 87 of length 5), so 'acyclic in kind' is not a usable test. Crate realisability (§2.1) is.

## 3. Layer 3 — requirement graph

**63 non-trivial SCCs** among 384 components (224 of 545 records sit inside a cycle). Families, classified by the areas involved:

| Family | SCCs |
|---|---|
| claim discipline — an obligation and the claim it licenses cite each other | 11 |
| central-invariant restatement (D-register duplication) | 10 |
| cross-area operational mutual definition | 9 |
| intra-area mutual definition (CANON) | 6 |
| intra-area mutual definition (CEK) | 5 |
| intra-area mutual definition (BUDGET) | 3 |
| intra-area mutual definition (PERSIST) | 3 |
| intra-area mutual definition (REPO) | 2 |
| intra-area mutual definition (CAP) | 2 |
| intra-area mutual definition (EFFECT) | 2 |
| intra-area mutual definition (ORDER) | 2 |
| intra-area mutual definition (TEST) | 2 |
| intra-area mutual definition (ACTOR) | 1 |
| intra-area mutual definition (MARSHAL) | 1 |
| intra-area mutual definition (CALC) | 1 |
| intra-area mutual definition (DUR) | 1 |
| intra-area mutual definition (PLANNER) | 1 |
| intra-area mutual definition (REF) | 1 |

### 3.1 The one that needs architectural review

**49 mutually dependent records** spanning 9 areas (ACTOR, BUDGET, CALC, CORE, DUR, EFFECT, HOST, PLANNER, RECOV): the request sequence, the issuance transaction, the escrow law, the host gate and the recovery classification each cite the others.

```
  REQ-ACTOR-005     REQ-ACTOR-010     REQ-ACTOR-011     REQ-ACTOR-012     REQ-ACTOR-013     REQ-ACTOR-017   
  REQ-ACTOR-021     REQ-ACTOR-022     REQ-BUDGET-001    REQ-BUDGET-002    REQ-BUDGET-005    REQ-BUDGET-019  
  REQ-BUDGET-022    REQ-BUDGET-023    REQ-BUDGET-024    REQ-BUDGET-025    REQ-CALC-009      REQ-CALC-010    
  REQ-CORE-002      REQ-CORE-005      REQ-CORE-006      REQ-CORE-007      REQ-CORE-011      REQ-DUR-001     
  REQ-DUR-002       REQ-DUR-003       REQ-DUR-004       REQ-DUR-013       REQ-DUR-014       REQ-EFFECT-005  
  REQ-EFFECT-010    REQ-EFFECT-013    REQ-EFFECT-015    REQ-EFFECT-016    REQ-EFFECT-017    REQ-EFFECT-018  
  REQ-EFFECT-019    REQ-EFFECT-024    REQ-EFFECT-026    REQ-EFFECT-027    REQ-EFFECT-032    REQ-HOST-001    
  REQ-HOST-002      REQ-HOST-003      REQ-HOST-007      REQ-HOST-010      REQ-HOST-011      REQ-PLANNER-017 
  REQ-RECOV-013   
```
Direct mutual citations inside it (each record appears in the other's DEPENDENCIES):

- `REQ-ACTOR-010` ⇄ `REQ-ACTOR-012`
- `REQ-BUDGET-005` ⇄ `REQ-BUDGET-019`
- `REQ-BUDGET-019` ⇄ `REQ-CORE-005`
- `REQ-BUDGET-022` ⇄ `REQ-RECOV-013`
- `REQ-BUDGET-024` ⇄ `REQ-DUR-013`
- `REQ-BUDGET-025` ⇄ `REQ-EFFECT-026`
- `REQ-BUDGET-025` ⇄ `REQ-EFFECT-032`
- `REQ-CORE-002` ⇄ `REQ-HOST-002`
- `REQ-CORE-007` ⇄ `REQ-DUR-001`
- `REQ-CORE-011` ⇄ `REQ-PLANNER-017`
- `REQ-DUR-002` ⇄ `REQ-DUR-003`
- `REQ-DUR-002` ⇄ `REQ-DUR-004`
- `REQ-DUR-002` ⇄ `REQ-EFFECT-016`
- `REQ-DUR-014` ⇄ `REQ-RECOV-013`

**Verdict.** Two readings are possible and the frozen text does not choose between them: (a) the 16-step request sequence (R-EFFECT-03) is normative and the durability transaction (R-DUR-02) is its step 14, or (b) the issuance transaction is normative and the sequence states when it is invoked. Conformance testing cannot separate them, so production and reference may pick different readings and still agree on every observable — until a crash point separates them (this is the same knot as U-02/U-17). Recommend naming R-EFFECT-03 the single normative ordering and restating R-DUR-02 as its step 14.

### 3.2 All non-trivial SCCs

| # | Size | Records | Areas |
|---|---|---|---|
| 1 | 49 | `REQ-ACTOR-005` … `REQ-RECOV-013` | ACTOR, BUDGET, CALC, CORE, DUR, EFFECT, HOST, PLANNER, RECOV |
| 2 | 17 | `REQ-CANON-036` … `REQ-TEST-031` | CANON, CLAIM, REF, SCOPE, TEST |
| 3 | 9 | `REQ-CALC-002` … `REQ-TRUST-009` | CALC, KERN, ORDER, TRUST |
| 4 | 6 | `REQ-CANON-009`, `REQ-CANON-011`, `REQ-CANON-019`, `REQ-CANON-022`, `REQ-CANON-023`, `REQ-CANON-025` | CANON |
| 5 | 6 | `REQ-CAP-002`, `REQ-CAP-003`, `REQ-CAP-004`, `REQ-CAP-007`, `REQ-CAP-012`, `REQ-CORE-004` | CAP, CORE |
| 6 | 5 | `REQ-ARCH-003`, `REQ-ARCH-005`, `REQ-COMPILE-001`, `REQ-COMPILE-002`, `REQ-ORDER-017` | ARCH, COMPILE, ORDER |
| 7 | 5 | `REQ-BUDGET-021`, `REQ-BUDGET-026`, `REQ-BUDGET-027`, `REQ-BUDGET-028`, `REQ-BUDGET-029` | BUDGET |
| 8 | 4 | `REQ-ACTOR-001`, `REQ-ACTOR-002`, `REQ-ACTOR-003`, `REQ-ACTOR-004` | ACTOR |
| 9 | 4 | `REQ-CEK-008`, `REQ-CEK-010`, `REQ-CEK-016`, `REQ-CEK-017` | CEK |
| 10 | 4 | `REQ-PERSIST-010`, `REQ-PERSIST-011`, `REQ-PERSIST-021`, `REQ-PERSIST-022` | PERSIST |
| 11 | 4 | `REQ-REPO-004`, `REQ-REPO-017`, `REQ-REPO-018`, `REQ-REPO-019` | REPO |
| 12 | 3 | `REQ-ACTOR-008`, `REQ-CALC-019`, `REQ-CAP-019` | ACTOR, CALC, CAP |
| 13 | 3 | `REQ-BUDGET-031`, `REQ-BUDGET-032`, `REQ-EFFECT-021` | BUDGET, EFFECT |
| 14 | 3 | `REQ-CANON-005`, `REQ-CANON-006`, `REQ-CANON-020` | CANON |
| 15 | 3 | `REQ-CANON-017`, `REQ-CANON-018`, `REQ-CANON-035` | CANON |
| 16 | 3 | `REQ-CAP-013`, `REQ-CAP-014`, `REQ-CAP-015` | CAP |
| 17 | 3 | `REQ-MARSHAL-001`, `REQ-MARSHAL-002`, `REQ-MARSHAL-009` | MARSHAL |
| 18 | 3 | `REQ-REF-012`, `REQ-TEST-007`, `REQ-TEST-011` | REF, TEST |
| 19 | 2 | `REQ-ACTOR-020`, `REQ-CLAIM-004` | ACTOR, CLAIM |
| 20 | 2 | `REQ-ARCH-004`, `REQ-COMPILE-013` | ARCH, COMPILE |
| 21 | 2 | `REQ-BUDGET-011`, `REQ-BUDGET-020` | BUDGET |
| 22 | 2 | `REQ-BUDGET-012`, `REQ-BUDGET-015` | BUDGET |
| 23 | 2 | `REQ-BUDGET-013`, `REQ-CLAIM-007` | BUDGET, CLAIM |
| 24 | 2 | `REQ-BUDGET-014`, `REQ-CAP-011` | BUDGET, CAP |
| 25 | 2 | `REQ-CALC-004`, `REQ-CLAIM-002` | CALC, CLAIM |
| 26 | 2 | `REQ-CALC-005`, `REQ-CALC-006` | CALC |
| 27 | 2 | `REQ-CALC-015`, `REQ-HOST-012` | CALC, HOST |
| 28 | 2 | `REQ-CANON-008`, `REQ-CANON-024` | CANON |
| 29 | 2 | `REQ-CANON-012`, `REQ-CANON-029` | CANON |
| 30 | 2 | `REQ-CANON-026`, `REQ-CANON-027` | CANON |
| 31 | 2 | `REQ-CAP-008`, `REQ-CAP-010` | CAP |
| 32 | 2 | `REQ-CAP-009`, `REQ-KERN-005` | CAP, KERN |
| 33 | 2 | `REQ-CAP-021`, `REQ-CLAIM-006` | CAP, CLAIM |
| 34 | 2 | `REQ-CEK-002`, `REQ-CLAIM-001` | CEK, CLAIM |
| 35 | 2 | `REQ-CEK-004`, `REQ-CEK-005` | CEK |
| 36 | 2 | `REQ-CEK-013`, `REQ-CEK-014` | CEK |
| 37 | 2 | `REQ-CEK-018`, `REQ-CEK-019` | CEK |
| 38 | 2 | `REQ-CEK-020`, `REQ-CEK-021` | CEK |
| 39 | 2 | `REQ-CLAIM-009`, `REQ-DUR-012` | CLAIM, DUR |
| 40 | 2 | `REQ-CLAIM-010`, `REQ-RECOV-012` | CLAIM, RECOV |
| 41 | 2 | `REQ-CLAIM-011`, `REQ-RECOV-015` | CLAIM, RECOV |
| 42 | 2 | `REQ-CLAIM-013`, `REQ-REF-013` | CLAIM, REF |
| 43 | 2 | `REQ-CLAIM-014`, `REQ-TEST-005` | CLAIM, TEST |
| 44 | 2 | `REQ-CORE-010`, `REQ-MARSHAL-003` | CORE, MARSHAL |
| 45 | 2 | `REQ-CORE-012`, `REQ-PLANNER-016` | CORE, PLANNER |
| 46 | 2 | `REQ-CORE-013`, `REQ-RECOV-002` | CORE, RECOV |
| 47 | 2 | `REQ-CORE-015`, `REQ-RECOV-011` | CORE, RECOV |
| 48 | 2 | `REQ-DUR-006`, `REQ-EFFECT-034` | DUR, EFFECT |
| 49 | 2 | `REQ-DUR-008`, `REQ-DUR-009` | DUR |
| 50 | 2 | `REQ-DUR-010`, `REQ-RECOV-004` | DUR, RECOV |
| 51 | 2 | `REQ-EFFECT-003`, `REQ-EFFECT-004` | EFFECT |
| 52 | 2 | `REQ-EFFECT-014`, `REQ-EFFECT-020` | EFFECT |
| 53 | 2 | `REQ-EFFECT-023`, `REQ-REF-014` | EFFECT, REF |
| 54 | 2 | `REQ-ORDER-002`, `REQ-ORDER-025` | ORDER |
| 55 | 2 | `REQ-ORDER-003`, `REQ-ORDER-022` | ORDER |
| 56 | 2 | `REQ-PERSIST-004`, `REQ-PERSIST-008` | PERSIST |
| 57 | 2 | `REQ-PERSIST-019`, `REQ-PERSIST-020` | PERSIST |
| 58 | 2 | `REQ-PLANNER-018`, `REQ-PLANNER-019` | PLANNER |
| 59 | 2 | `REQ-REF-004`, `REQ-SCOPE-011` | REF, SCOPE |
| 60 | 2 | `REQ-REF-007`, `REQ-REF-008` | REF |
| 61 | 2 | `REQ-REPO-006`, `REQ-REPO-007` | REPO |
| 62 | 2 | `REQ-TEST-009`, `REQ-TEST-010` | TEST |
| 63 | 2 | `REQ-TEST-017`, `REQ-TEST-029` | TEST |

## 4. Layer 4 — section graph

9 SCCs, **1 non-trivial**.

- **{S-07, S-08, S-09, S-10, S-11, S-12, S-13, S-15, S-16, S-17, S-18, S-19, S-20, S-21, S-22, S-23}** (16 sections) — the loop closes through the three packaging sections (`S-21` test infrastructure, `S-22` repository structure, `S-23` milestones): `spec/04` §A draws `S-22 -> S-07`, `S-22 -> S-10`, `S-22 -> S-18` dashed (repository structure is *hosted by* the semantics it packages) while `S-21 -> S-23 -> S-22` runs forward, so the section graph is cyclic even though `spec/04` §B reports 'no circular dependencies detected among frozen objects' (that claim is about the semantic-object graph, not this one).

Forward references (a section that uses material defined later): 5 of 34 — `S-22 -> S-07` (S-07 depends on S-22, defined 15 sections later); `S-22 -> S-10` (S-10 depends on S-22, defined 12 sections later); `S-17 -> S-13` (S-13 depends on S-17, defined 4 sections later); `S-22 -> S-18` (S-18 depends on S-22, defined 4 sections later); `S-23 -> S-22` (S-22 depends on S-23, defined 1 section later).

